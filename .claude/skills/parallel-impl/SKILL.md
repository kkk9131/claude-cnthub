---
name: parallel-impl
description: 並列実装を完全自動化。Plans.md/Tasks.mdから依存分析→Worktree作成→bypass+TDDで実装開始。引数不要。
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task, TodoWrite, Skill
---

# /parallel-impl - 自動並列実装

引数なしで実行。Plans.md/Tasks.md を自動分析し、並列実装を開始。

## 使い方

```
/parallel-impl
```

これだけ。オプション不要。

---

## 自動実行フロー

```
/parallel-impl
    ↓
┌─────────────────────────────────────────────┐
│ Step 1: 依存分析                              │
│   - Plans.md / Tasks.md を読み込み           │
│   - TODO タスクを抽出                        │
│   - 依存グラフを構築                         │
│   - 並列可能グループを特定                    │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 2: Worktree 作成                        │
│   - 並列グループごとに worktree を作成        │
│   - git worktree add ../{project}-{group}   │
│   - 設定ファイル (.claude/, .env) をコピー   │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 3: Agent 起動 (bypass + TDD)            │
│   - 各 worktree で Task Agent を起動         │
│   - bypass permissions: ON                  │
│   - /work スキル + TDD ワークフロー使用       │
│   - バックグラウンド実行                     │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ Step 4: 監視 & 統合                          │
│   - 依存シグナル監視                         │
│   - 完了後に競合チェック                     │
│   - マージ → Worktree 削除                   │
└─────────────────────────────────────────────┘
```

---

## 実装手順

### Step 1: 依存分析

```typescript
// Plans.md または Tasks.md を読み込み
const planFile = await findPlanFile(); // Plans.md or Tasks.md

// TODO タスクを抽出（テーブル形式をパース）
const tasks = parseTasks(planFile);

// 依存グラフを構築
const graph = buildDependencyGraph(tasks);

// 並列可能グループを特定
const groups = findParallelGroups(graph);
// 例: [[L-02, L-03], [M-03]] → 2つの worktree
```

> 詳細: [references/01-dependency-analysis.md](references/01-dependency-analysis.md)

### Step 2: Worktree 作成

```bash
# 各グループ用に作成
for group in groups:
  git worktree add ../{project}-{group} -b feature/{group}-{date}
  cp -r .claude/ ../{project}-{group}/
done
```

> 詳細: [references/02-worktree-setup.md](references/02-worktree-setup.md)

### Step 3: Agent 起動

```typescript
Task({
  subagent_type: "general-purpose",
  run_in_background: true,
  prompt: `
    作業ディレクトリ: {WORKTREE_PATH}
    タスク: {TASK_LIST}

    ## 実行方法
    1. /work スキルを使用してタスクを実行
    2. TDD: テスト作成 → 実装 → テスト実行
    3. 完了したら Plans.md を [x] に更新

    ## 権限
    bypass permissions: ON
    全ツール自動承認
  `
})
```

> 詳細: [references/03-agent-management.md](references/03-agent-management.md)

### Step 4: 統合

```bash
# 完了待ち
TaskOutput(agentIds, { block: true })

# 競合チェック
git diff main --name-only  # 各 worktree

# マージ
git checkout main
git merge --no-ff feature/{group}

# クリーンアップ
git worktree remove ../{project}-{group}
```

> 詳細: [references/04-integration.md](references/04-integration.md)

---

## 出力例

```
🔍 依存分析中...

📊 分析結果
┌────────────────────────────────────────┐
│ 並列グループ A: L-02, L-03             │
│ 並列グループ B: M-03                   │
│ クロス依存: なし                       │
└────────────────────────────────────────┘

🔧 Worktree 作成中...
  ✅ ../claude-cnthub-groupA (L-02, L-03)
  ✅ ../claude-cnthub-groupB (M-03)

🚀 Agent 起動 (bypass: ON, TDD: ON)
  ✅ Group A Agent started
  ✅ Group B Agent started

📡 実装中... (バックグラウンド)
```

---

## 関連スキル

- `/work` - タスク実行（Agent 内で使用）
- `/tdd` - TDD ワークフロー（Agent 内で使用）
- `/gtr` - Worktree 操作（手動時）
